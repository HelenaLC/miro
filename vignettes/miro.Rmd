---
title: "`miro`: Visuals for Spatial (Transcript)Omics"
author:
- name: Helena L. Crowell
  affiliation: National Center for Genomics Analysis (CNAG), Barcelona, Spain
output:
  BiocStyle::html_document
abstract: |
  Description of your vignette
vignette: |
  %\VignetteIndexEntry{miro}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
date: "`r format(Sys.Date(), '%B %d, %Y')`"
package: "`r BiocStyle::pkg_ver('miro')`"
---

# Preamble

## Introduction

## Dependencies

```{r load-libs}
library(OSTA.data)
library(SpatialExperiment)
library(SpatialExperimentIO)
```

```{r load-data, message=FALSE, warning=FALSE}
# retrieve Xenium dataset
id <- "Xenium_HumanColon_Oliveria"
pa <- OSTA.data_load(id)
dir.create(td <- tempfile())
unzip(pa, exdir=td)
(spe <- readXeniumSXE(td))
# crop for demo
xy <- spatialCoords(spe)
sub <- spe[, 
    xy[,1]>1000 & xy[,1]<2000 & 
    xy[,2]<2000 & xy[,2]>1500 ]
colRanges(spatialCoords(sub))
```

# Centroids

```{r fig.width=4, fig.height=3.5}
miro(spe, dat_aes=list(stroke=0, size=0.2, col="total_counts"), dat_t=log10)
```

# Polygons

```{r}
p <- "cell_boundaries"
miro(sub, pol=p, pol_aes=list(fill="cell_area"))
```

# Molecules

```{r fig.height=3.5, fig.width=4}
m <- "transcripts"
miro(sub, mol=m, keys="CXCL5")
```

# Layering

# Appendix

## Session info {- .smaller}

<style type="text/css">
.smaller {
  font-size: 10px
}
</style>

```{r si, echo=FALSE}
sessionInfo()
```
